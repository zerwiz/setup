# This file is responsible for configuring your application
import Config

config :logger, level: :warning

# API server port (used when running: mix run -e "AiDevSuiteTui.API.start()")
config :ai_dev_suite_tui, :api_port, 41_434
